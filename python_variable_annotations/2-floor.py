#!/usr/bin/env python3
"""
Function which takes a float as argument
and returns the floor of the float.
"""


import math


def floor(n: float) -> int:
    """
    Returns floor of a float.

    Args:
        n: number to floor.

    Returns:
        int: floor of the float
    """
    return math.floor(n)
